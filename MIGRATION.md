# 메인 이전 절차

되돌리기 쉬운 순서로 배치했다. 각 단계마다 되돌리는 법을 같이 적었다.
**메인 페이지는 지우지 않는다.** 순서만 밀려날 뿐 그대로 남는다.

---

## 0. 사전 준비 (완료)

- 실사이트 8개 페이지 백업 → `backup/20260820/`
- 이전 전 점검에서 링크 두 곳 결함 발견 → `theme.html` 에 보정 코드 반영

---

## 1. 랜딩 최신본 반영

`/38` 이 아직 옛 버전이다 — 카운터가 **270** 으로 남아 있다(최신본은 500).

```
/38 → 코드 위젯 우클릭 → 코드 설정 → ⌘A → ⌘V → 저장 → 게시
```

붙여넣을 것: `dist/imweb.html`

**되돌리기**: 이전 코드로 다시 붙여넣기 (git 에 모든 판이 남아 있다)

---

## 2. 공통 테마 넣기

```
환경설정 → SEO → 공통 코드삽입 → Body Code → theme.html 내용 붙여넣기 → 저장
```

사이트 전체가 랜딩 톤이 된다. 링크 보정 코드도 여기 들어 있다.

**되돌리기**: Body Code 를 비우면 즉시 원상복구. 가장 안전한 단계다.

확인할 것
- `/project` `/about` `/food` 바탕이 어두워지고 네비가 Anton 으로 바뀌는지
- 카테고리 버튼이 형광 막대가 아니라 조용한 알약인지
- 페이지 맨 아래 흰 띠가 사라졌는지

---

## 3. 대표 이미지 (OG)

```
환경설정 → 일반 → 대표 이미지 → og/instudio-og.png (1200×630) 업로드
```

카카오톡은 캐시가 남아 바로 안 바뀐다.
Kakao Developers → 도구 → 디버거 에서 URL 초기화 필요.

**되돌리기**: 이미지 삭제

---

## 4. 홈 바꾸기 ← 실제 이전

```
디자인 모드 → 메뉴 관리 → '/38' 메뉴를 상단 메뉴의 맨 위로 드래그
```

아임웹은 **상단 메뉴의 최상단에 있는 메뉴를 홈으로** 삼는다.
기존 메인은 삭제되지 않고 두 번째로 밀린다.

**되돌리기**: 기존 메인 메뉴를 다시 맨 위로 드래그. 그게 전부다.

---

## 5. 확인 후 정리

홈이 정상 동작하는 걸 확인한 뒤에만 한다.

- 기존 메인 메뉴 → **숨김**(삭제 아님)
- `/34` `/37` 둘 중 하나 삭제 (내용이 완전히 같다)
- `/3` 삭제 검토 (`/project` 와 중복)
- 메뉴별 페이지 제목·설명 입력 → `docs-seo.md`

---

## 이전 전 점검에서 나온 것

| 항목 | 상태 |
|---|---|
| 랜딩 내부 링크 8개 | 정상 (`/project` `/food` 등 전부 200) |
| JS 오류 | 없음 (PC·폰) |
| 페이지 높이 | PC 10.8vh · 폰 9.8vh |
| 뒤쪽 아임웹 잔재 | `trimTail()` 이 2개 섹션 정리 |
| **모바일 로고 → `/1`** | **결함** — `/1` 은 옛 메인의 번호 별칭이라 홈이 바뀌어도 옛 페이지로 간다. theme.html 이 `/` 로 고친다 |
| **헤더 Contact → `#s2025…`** | **결함** — 옛 메인에만 있는 앵커라 새 홈에서는 못 간다. theme.html 이 현재 페이지의 문의 폼으로 보낸다 |

두 결함 모두 주입 테스트로 수정 확인했다: `/1` 링크 1개 → 0개, 로고 전부 `/`, Contact 앵커 4개 모두 처리됨.
